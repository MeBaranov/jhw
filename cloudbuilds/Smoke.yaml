steps:
  - name: gcr.io/cloud-builders/gcloud
    entrypoint: bash
    args:
      - "-c"
      - |
        res=$(curl $_URI/hello-world); if (echo $res | grep "Hello World"); then echo success; else echo ERROR: unexpected curl result: $res; exit 1; fi